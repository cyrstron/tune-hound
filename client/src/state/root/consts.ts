export const APP_ROOT = '@app';

export const INIT_APP = `${APP_ROOT}/INIT` as const;

export const INJECT_DYNAMIC_SAGA = `${APP_ROOT}/INJECT_DYNAMIC_SAGA` as const;
export const EJECT_DYNAMIC_SAGA = `${APP_ROOT}/EJECT_DYNAMIC_SAGA` as const;

export const INJECT_DYNAMIC_REDUCER = `${APP_ROOT}/INJECT_DYNAMIC_REDUCER` as const;
export const EJECT_DYNAMIC_REDUCER = `${APP_ROOT}/EJECT_DYNAMIC_REDUCER` as const;
